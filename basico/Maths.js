//Pi
//console.log(Math.PI)
//Euler
//console.log(Math.E)

let x = 3
let y = 2
let z

//z = Math.round(x)     //Arredonda para o inteiro mais próximo
//z = Math.floor(x)     //Arredonda para baixo
//z = Math.ceil(x)      //Arredonda para cima
//z = Math.trunc(x)     //Remove o decimal
//z = Math.pow(x, y)    //Eleva a potência y
//z = Math.sqrt(x)      //Raiz quadrada de x
//z = Math.log(x)       //Logaritmo natural de x
//z = Math.sin(x)       //Retorna o seno de x, onde x representa radianos
//z = Math.cos(x)       //Retorna o cosseno de x, onde x representa radianos
//z = Math.tan(x)       //Retorna o tangente de x, onde x representa radianos
//z = Math.abs(x)       //valor absoluto (positivo) de x
//z = Math.sign(x)      //retorna 1, -1, 0
console.log(z)

z = 1
let max = Math.max(x, y, z)
let min = Math.min(x, y, z)

console.log(min)

let randomNum = Math.random();

console.log(randomNum)

